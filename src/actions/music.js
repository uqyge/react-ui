import { PinDropSharp } from "@material-ui/icons";
import React from "react";

const Music = (props) => {
  return (
    <button
      onClick={() => {
        console.log('music')
      }}
    >
      Music
    </button>
  );
};

export default Music;
